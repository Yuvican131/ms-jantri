
"use client"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function LedgerRecord() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Ledger Record</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the placeholder for the Ledger Record content.</p>
      </CardContent>
    </Card>
  )
}
